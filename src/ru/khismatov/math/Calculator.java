/**
 * - Задание 6: Сложение.
 *   - Реализует интерфейс {@link ICalculator}.
 *   - Содержит метод для суммирования набора числовых значений в вещественной форме.
 *     Примеры использования:
 *     - 2 + 3/5 + 2.3
 *     - 3.6 + 49/12 + 3 + 3 2
 *     - 1/3 + 1
 *
 * - Задание 10: Возведение в степень.
 *   - Содержит метод для возведения числа X в степень Y, где X и Y передаются как строки.
 *   - Использует короткие имена статических методов для Integer#parseInt(String) и
 *   Math#pow(double, double).
 */

package ru.khismatov.math;

import static java.lang.Integer.parseInt;
import static java.lang.Math.pow;

/**
 * Реализация калькулятора, поддерживающая операции суммирования и возведения в степень.
 * Реализует интерфейс ICalculator для работы с числами различных типов.
 * 
 * Особенности:
 * - Реализует суммирование произвольного количества чисел
 * - Поддерживает работу с целыми числами, дробями и числами с плавающей точкой
 * - Предоставляет статический метод для возведения в степень
 * - Использует короткие имена для статических методов Integer.parseInt и Math.pow
 */
public class Calculator implements ICalculator {
  /**
   * Вычисляет результат возведения числа в степень.
   * Оба параметра должны быть строками, содержащими целые числа.
   *
   * @param exp основание степени (целое число в виде строки)
   * @param power показатель степени (целое число в виде строки)
   * @return результат возведения в степень
   */
  public static double calculate_power(String exp, String power) {
    return pow(parseInt(exp), parseInt(power));
  }

  /**
   * Суммирует произвольное количество числовых значений.
   * Все числа преобразуются в тип double перед суммированием.
   *
   * @param numbers набор чисел для суммирования
   * @return сумма всех чисел в виде числа с плавающей точкой
   */
  @Override
  public double sum(Number... numbers) {
    double sum = 0;
    for (Number number : numbers) {
      sum += number.doubleValue();
    }
    return sum;
  }
}
